import axios from 'axios';

const useFetchTopics = async () => {
  const { data } = await axios.get('https://heady-star-waste.glitch.me/topic')

  return data.data;
};

export default useFetchTopics;
